# Ignore EVERYTHING at the project root by default.
/*

# Whitelist only what should land in the remote.
!/.gitignore
!/.github
!/backend
!/frontend
!/docker-compose.yml
!/.env.example
!/TESTING.md
!/README.md

# Always exclude the real .env even if a future whitelist glob matches it.
.env
